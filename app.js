// <========= Chp 1 (ALERTS) =========> //

// ========= Task 1 ========= //

alert("Muhammad Shahzaib");
alert("Khan");
alert("abc@gmail.com");
alert("031234456457");
alert("Hello World");

// ========= Task 2 ========= //

/* Correct the statement: alert"You're learning JavaScript!"; */

alert("You are learning JavaScript!");

// ========= Task 3 ========= //

/* Code an alert statement displaying any message you like. */

alert("I will try to be the best programmer");

// <========= Chp 3 (Variables for string) =========> //

// ========= Task 1 ========= //

/* Declare any variable in the camelCase format. */

var firstName;

// ========= Task 2 ========= //

/* Declare a variable of your choice without defining it. Then, in a second statement, assign it a string of your choice. */

var info;

info = "My name is Muhammad Shahzaib Khan";

// ========= Task 3 ========= //

/* Declare the variable team Name and Alert your Team name. */

var teamName = "Team Pakistan";

alert(teamName);

// ========= Task 4 ========= //

/* This statement has already been coded. var bestMan = "Charlie"; Assign the variable a new string. */

var bestMan = "Charlie";

bestMan = "Jeff bezos";

// <========= Chp 3 (Variables for numbers) =========> //

// ========= Task 1 ========= //

/* Declare a variable “caseQty” */

var caseQty;

// ========= Task 2 ========= //

/* Assign to the variable caseQty, which has already been declared, the value 144. */

caseQty = 144;

// ========= Task 3 ========= //

/* Rewrite this statement so the variable can be used in a math operation. var num = "9"; */

var num = 9;

// ========= Task 4 ========= //

/* In one statement declare a variable. In a second statement assign it the sum of 2 numbers.*/

var no;

no = 6;

// ========= Task 5 ========= //

/* What is the value of orderTotal?
var merchTotal = 100;
var shippingCharge = 10;
var orderTotal = merchTotal + shippingCharge;
Try it yourself. */

var merchTotal = 100;
var shippingCharge = 10;
var orderTotal = merchTotal + shippingCharge;
console.log(orderTotal);

// ========= Task 6 ========= //

/* In the first statement declare a variable and assign it a number. In the second statement, change the value of the variable by adding it together with a number. */

var number1 = 10;
var number2 = 10 + 5;

console.log(number2);

// <========= Chp 4 (Variable names Legal and Illegal) =========> //

// ========= Task 1 ========= //

/* Correct this statement.
var product cost = 3.45; */

var productCost = 3.45;

// ========= Task 2 ========= //

/* Rewrite this using camelCase.
var Nameofband; */

var nameOfBand;

// ========= Task 3 ========= //

/* In a single statement declare a legally-named variable and assign a number to it. */

var legally_named = 150;

// ========= Task 4 ========= //

/* Declare a variable that is a combination of your first and last names. Use camelCase. */

var firstName = "Muhammad Shahzaib";
var lastName = "Khan";
var fullName = firstName + " " + lastName;

// ========= Task 5 ========= //

/* List the legal and Illegal Variables. */

// var name; // Legal
// var full_Name; // Legal
// var a123; // legal
// var last$name; //Legal

// var 123a; // Illegal
// var &abc; // Illegal
// var full-Name; // Illegal
// var for; // Illegal reserved keyword

// <========= Chp 5 (Math Expression I) =========> //

// ========= Task 1 ========= //

/* What is the name and symbol of the arithmetic operator that gives you the remainder when one number is divided by another? */

// Name of the arithmetic operator is "MODULUS" and the sign is "%" that gives you the remainder when one number is divided by another//

// ========= Task 2 ========= //

/* What is the value of num?
var num = 20 % 6; */

var num = 20 % 6;
// value is 2. //

// ========= Task 3 ========= //

/* In a single statement, declare the variable largeNum and assign it the result of 1,000 multiplied by 2,000. */

var largeNum = 1000 * 2000;

// ========= Task 4 ========= //

/* Assign to a variable the value represented by one variable subtracted from the value represented by another variable */

var num1 = 100;
var num2 = 450;

var sum = num2 - num1;

// ========= Task 5 ========= //

/* Assign to a variable the remainder when one number is divided by another. The variable hasn't been declared beforehand. Make up
the variable name. */

var num1 = 20;
var num2;
5;

var result = num1 % num2;

// ========= Task 6 ========= //

/* Code an alert that displays the result of a multiplication on 2 numbers. */

alert(2 * 5);

// <========= Chp 6 (Math Expression II) =========> //

// ========= Task 1 ========= //

/* Code a short form of x = x + 1; Use either of the two legal expressions. */

var x = 1;
++x;

// ========= Task 2 ========= //

/* If x has a value of 100, what is the fastest way to reduce it to 99 with a math expression? */

var x = 100;
--x; // this is the fastest way to reduce 100 to 99 with a math expression.

// ========= Task 3 ========= //

/* var x = 50;
   var y = x++;
What is the value of y? */

// value of y is 50;

// ========= Task 4 ========= //

/* var y = 50;
var z = --y;
What is the value of z? */

// Value of z is 49;

// ========= Task 5 ========= //

/* In a single statement, decrement num and assign its original value to newNum. */

var num = 20;

var newNum = --num;

// ========= Task 6 ========= //

/* In a single statement add 1 to a variable and assign its original value to another variable. */

var num = 20;

var newNum = ++num;

// ========= Task 7 ========= //

/* Assign a number value to a variable.Increment the variable.Display the new value in an alert */

var num = 6;

++num;
alert(num);

// <========= Chp 7 (Math Expression III) =========> //

// ========= Task 1 ========= //

/* var calculatedNum = 2 + (2 * 6); What is the value of calculatedNum? */

// The value of the calculatedNum is 14.

// ========= Task 2 ========= //

/* var calculatedNum = (2 + 2) * 6; What is the value of calculatedNum? */

// The value of the calculatedNum is 24.

// ========= Task 3 ========= //

/* var calculatedNum = (2 + 2) * (4 + 2); What is the value of calculatedNum? */

// The value of the calculatedNum is 24.

// ========= Task 4 ========= //

/* var calculatedNum = ((2 + 2) * 4) + 2; What is the value of calculatedNum? */

// The value of the calculatedNum is 18.

// ========= Task 5 ========= //

/* Write a statement that assigns to cost the result of 2 + 2 * 4 + 10, clarified with parentheses, producing 56. */

var cost = (2 + 2) * (4 + 10);
// result of the cost is 56.

// ========= Task 6 ========= //

/* Write a statement that assigns to units the result of 2 + 2 * 4 + 10, clarified with parentheses, producing 20. */

var units = 2 + 2 * 4 + 10;
// Output of units is 20.

// ========= Task 7 ========= //

/* Write a statement that assigns to pressure the result of 4 / 2 * 4, clarified with parentheses, producing 5. */

var pressure = 4 / (2 * 4);
var pressure = (4 / 2) * 4;
// Output can either be 2 or 8.

// <========= Chp 8 (Concatenating Text Strings) =========> //

// ========= Task 1 ========= //

/* var num = "2" + "2"; What is the value of num? Include quotation marks. */

// Value of num is 22.

// ========= Task 2 ========= //

/* message = ("Hello," + "Dolly");
What is the value of message? (Include the quotation marks.) Alert the statement */

message = "Hello," + "Dolly";

alert(message);
// Value of the message is Hello,Dolly.

// ========= Task 3 ========= //

/* alert("33" + 3); What message displays in the alert box? */

alert("33" + 3);
// Output is 333.

// ========= Task 4 ========= //

/* Write an alert that displays the concatenation of the two parts of "Pakistan Zindabad". */

var first = "Pakistan";
var second = "Zindabad";

alert(first + " " + second);

// ========= Task 5 ========= //

/* Write a statement that assigns to a variable the concatenation of a string with a number */

var unit = "Hello" + 100;

// ========= Task 6 ========= //

/* Assign strings to two variables. Then concatenate them and assign the result to a third variable. */

var message1 = "Hello";
var message2 = "World";
var message3 = message1 + " " + message2;
console.log(message3);

// <========= Chp 9 (Prompts) =========> //

// ========= Task 1 ========= //

/* Code a prompt with the message "Enter first name". The user's response is assigned to firstName. */

var firstName = prompt("Enter first name");

// ========= Task 2 ========= //

/* Code a prompt with the message "Country?" and the default answer "China". The user's response is assigned to country. */

var country = prompt("Country?", "China");

// ========= Task 3 ========= //

/* Correct this statement
var yourName = prompt(Enter Your Name"); */

var yourName = prompt("Enter Your Name"); // starting " was missing

// ========= Task 4 ========= //

/* Code a prompt that specifies a string as the message Include a default input. */

var message = prompt(
  "Enter your message",
  "I wil try my best to become the best programmer"
); // by default prompt takes the value in string.

// ========= Task 5 ========= //

/* Assign strings to two variables. Code a prompt specifying the first variable as the message and the second variable as the default response. Assign the user's response to a third variable. */

var enter = "Enter your name";
var name = "Shahzaib Khan";

var user = prompt(enter, name);

// ========= Task 6 ========= //

/* Display a prompt, including both a message and a default response. Display the user's response in an alert. */

var user = prompt("Enter your age", "22");
alert(user);

// <========= Chp 10 (if statments) =========> //

// ========= Task 1 ========= //

/* var city = "Karachi"
if (city = "Karachi") {
console.log("The City OF Lights")
Correct the above statement:
Also try this statement by yourself */

var city = "Karachi";
if (city == "Karachi") {
  console.log("The City Of Lights");
}

// ========= Task 2 ========= //

/* This is the first line of an if statement:
if (x === y) {
Complete the statement. If the condition is true, display a box that asks the user value of z? and assign it to another variable. */

var z = 6;
if (z == 6) {
  var user = prompt("What is the value of z");
  var ans = user;
  console.log(ans);
}

// ========= Task 3 ========= //

/* Code an if statement that tests if ZipCode is "10010" so, Alert that "Karachi". if not then alert ("Please write correct city") */

var zipCode = prompt("Enter the Zip Code");
if (zipCode == "10010") {
  alert("Karachi");
} else {
  alert("Please write correct zip code");
}

// ========= Task 4 ========= //

/* Code an if statement. Test whether a variable has a particular numerical value. If so, assign a new value to that variable, as in
x = 1; */

var x = 5;
if (x === 5) {
  x = 2;
  console.log(x);
} else {
  console.log("Please enter a numeric value");
}

// <========= Chp 11 (Comparison Operators) =========> //

// ========= Task 1 ========= //

/* Code the first line of an if statement that tests whether one variable is unequal to another. (Use !) */

var x = 6;
var z = 9;
if (x != z) {
  console.log("X is not equal to Z");
}

// ========= Task 2 ========= //

/* Code the first line of an if statement that tests whether the value represented by a variable is greater than or equal to the value represented by another variable. */

var x = 9;
var y = 5;
if (x >= y) {
  console.log("X is greater than or equal to the y");
}

// ========= Task 3 ========= //

/* Code an if statement. Test whether a variable is unequal to a particular number. If so, assign a number to that variable. */

var x = 6;
if (x != 9) {
  x = 8;
  console.log(x);
}

// ========= Task 4 ========= //

/* Code an if statement that tests whether a number is unequal to a different number. If the condition is true (it will be), display a congratulations alert. */

var a = 10;
var b = 5;
if (a != b) {
  alert("Congratualtions");
} else {
  alert("a is equal to b");
}

// ========= Task 5 ========= //

/* Code a prompt asking for your first name.Code an if statement that tests whether the name you entered is unequal to another name. If the condition is true (it will be), display an alert that says "No match" */

var firstName = prompt("Enter your first name");
if (firstName != "Shahzaib") {
  alert("No Match");
} else {
  alert("Matched");
}

// <========= Chp 12 (if...else and else if statements) =========> //

// ========= Task 1 ========= //

/* Code an if statement that tests whether the value represented by a variable is greater than or equal to the value represented by another variable. If so, display an alert. If not, display a different alert. */

var a = 10;
var b = 5;
if (a >= b) {
  alert("A is greater than or equal to B");
} else {
  alert("A is less than B");
}

// ========= Task 2 ========= //

/* Write a program using if else and else if statement which take marks from user. And the program will calculate your percentage and give you grade A/C to Your percentage. (MARKSHEET) */

var english = +prompt("Enter English Marks");
var maths = +prompt("Enter Maths Marks");
var science = +prompt("Enter Science Marks");
var urdu = +prompt("Enter Urdu Marks");
var computer = +prompt("Enter Computer Marks");

var totalMarks = english + maths + science + urdu + computer;
var calculate = (totalMarks / 500) * 100;

if (calculate >= 80) {
  console.log(
    "Your percantage is " +
      calculate +
      " % " +
      " " +
      "Congratulation you got an A Grade"
  );
} else if (calculate >= 60 && calculate < 80) {
  console.log(
    "Your percantage is " +
      calculate +
      " % " +
      " " +
      "Congratulation you got an B Grade"
  );
} else if (calculate >= 40 && calculate < 60) {
  console.log(
    "Your percantage is " +
      calculate +
      " % " +
      " " +
      "Congratulation you got an C Grade"
  );
} else if (calculate < 40) {
  console.log("Your percantage is " + calculate + " % " + " " + "You Failed");
}

// ========= Task 3 ========= //

/* This is the if statement that begins the code.
if (a === 10) {
alert("a is 10");
}
If a isn't 10, display an alert that says The correct value of a is
____
Note: Try this by yourself */

var a = 15;
if (a === 10) {
  alert("A is 10");
} else {
  alert("The correct value of a is 15");
}

// ========= Task 4 ========= //

/* Prompt the user to enter a city.
If the city is Karachi, display an alert acknowledging it is Karachi.
If not, check to see if it's Lahore.
If it is, display an alert acknowledging it's Lahore.
Otherwise, display a different alert. */

var city = prompt("Enter your City", "Karachi");
if (city == "Karachi") {
  alert("It is Karachi");
} else if (city == "Lahore") {
  alert("It is Lahore");
} else {
  alert("City not found");
}

// <========= Chp 13 (Testing sets of conditions) =========> //

// ========= Task 1 ========= //

/* Code the first line of an if statement that tests whether both are true: a has the same value as b and c has the same value as d. */

var a = 6;
var b = 6;
var c = 9;
var d = 9;
if (a == b && c == d) {
  console.log("All values are equal");
}

// ========= Task 2 ========= //

/* Code the first line of an if statement that tests whether either or both are true: a has the same value as b or c doesn't have the same value as d. */

var a = 6;
var b = 6;
var c = 9;
var d = 10;
if (a == b || c == d) {
  console.log("true");
}

// ========= Task 3 ========= //

/* Code the first line of an if statement that tests whether I. name is either "Hamza" or "Arsalan". II. age is Less than 60. */

var name = "Arsalan";
var age = 38;
if ((name == "Hamza" || name == "Arsalan") && age < 60) {
  console.log("allowed");
} else {
  console.log("Not allowed");
}

// ========= Task 4 ========= //

/* Declare two variables and assign them number values. If the first variable is less than the second variable or greater than the second variable, display an alert. */

var a = 10;
var b = 20;
if (a < b || a > b) {
  alert("true");
} else {
  alert("false");
}

// ========= Task 5 ========= //

/* Declare 2 variables. Assign one of them your first name and the other one your last name. Code 2 prompts, asking for your first and your last name. If your answers match the two variables, display an alert. */

var firstName = "Muhammad Shahzaib";
var lastName = "Khan";

var user1 = prompt("Enter your first name");
var user2 = prompt("Enter your last name");

if (firstName == user1 && lastName == user2) {
  alert("Matched");
} else {
  alert("Not Matched");
}

// <========= Chp 14 (If statements nested) =========> //

// ========= Task 1 ========= //

/* Code an if statement enclosing a nested if. If password is not empty, then check if password is not greater than 5 , then display an alert that says "Password must be greater than 5" if greater than 5 then Alert "OK". */

var pass = 4;
if (pass == "") {
  alert("Please enter pass");
}
if (pass < 5) {
  alert("Password must be greater than 5");
}
if (pass > 5) {
  alert("OK");
}

// ========= Task 2 ========= //

/* Try this statement by yourself
if (a === 1) {
if (c === "Max") {
alert("OK");
}
} */

var a = 1;
var c = "Max";
if (a === 1) {
  if (c === "Max") {
    alert("OK");
  }
}

// ========= Task 3 ========= //

/* Code the first line of an if statement that avoids the nesting above by testing for multiple conditions.
if (a === 1) {
if (c === "Max") {
alert("OK");
}
} */

var a = 1;
var c = "Max";
if (a === 1 && c === "Max") {
  alert("OK");
}

// ========= Task 4 ========= //

/* Declare two variables and assign them the same number value.Test two conditions, using nested if statements. Test whether the first variable equals the second variable and also whether it is less than or equal to the second variable. If the test passes—and it will—display an alert message. */

var x = 50;
var z = 50;
if (x === z) {
  if (x <= z) {
    alert("OK");
  }
}

// <========= Chp 15 (Array I) =========> //

// ========= Task 1 ========= //

/* Declare an empty array. */

var arr = [];

// ========= Task 2 ========= //

/* Code an array with 1 string element */

var arr = ["Mango"];

// ========= Task 3 ========= //

/* var alphabet = ["h","i","j","k"]. Now print the letter “j” in alert using array index */

var alphabet = ["h", "i", "j", "k"];

alert(alphabet[2]);

// ========= Task 4 ========= //

/* var alphabet=["h","i","j","k", “l”,”m”, “n”, “o”]. Find the total length of array. */

var alphabet = ["h", "i", "j", "k", "l", "m", "n", "o"];

console.log("length of the array is " + alphabet.length);

// ========= Task 5 ========= //

/* Declare an array with one element and Add a second element with index in array. Create an alert whose message is the new element. */

var arr = ["Harry"];
arr[1] = "Elon";
console.log(arr[1]);

// <========= Chp 16 (Array II) =========> //

// ========= Task 1 ========= //

/* Code an array with 1 string element.Add an additional element to the array using push. Create an alert whose message is the last element. */

var arr = ["Mango"];
arr.push("Apple");
alert(arr[arr.length - 1]);

// ========= Task 2 ========= //

/* var Alphabet=["h","i","j","k"] Remove the last element from the array Alphabet. */

var Alphabet = ["h", "i", "j", "k"];
Alphabet.pop();
console.log(Alphabet);

// ========= Task 3 ========= //

/* var Alphabet=["h","i","j","k"] Add a new element, a number, to the end of an array. */

var Alphabet = ["h", "i", "j", "k"];
Alphabet.push(6);
console.log(Alphabet);

// <========= Chp 16 (Array III) =========> //

// ========= Task 1 ========= //

/* var sizes = ["S", "M", "XL", "XXL", "XXXL"]. Remove the first element of an array. */

var sizes = ["S", "M", "XL", "XXL", "XXXL"];
sizes.shift();
console.log(sizes);

// ========= Task 2 ========= //

/* var sizes = ["S", "M", "XL", "XXL", "XXXL"]. Add three number elements to the beginning of an array. */

var sizes = ["S", "M", "XL", "XXL", "XXXL"];
sizes.unshift(1, 2, 3);
console.log(sizes);

// ========= Task 3 ========= //

/* 3. Declare an array with one element. Add a second element to the beginning of the array. Create an alert whose message is the new first element. */

var arr = ["Apex Legends"];
arr.unshift("CS GO");
alert(arr[0]);

// ========= Task 4 ========= //

/* var sizes = ["S", "M", "XL", "XXL", "XXXL"].Insert "L" into the array between "M" and "XL". */

var sizes = ["S", "M", "XL", "XXL", "XXXL"];
sizes.splice(2, 0, "L");
console.log(sizes);

// ========= Task 5 ========= //

/* var sizes = ["S", "M", "XL", "XXL", "XXXL"]. Copy the first 3 sizes of the array and put them into a new array, regSizes. */

var sizes = ["S", "M", "XL", "XXL", "XXXL"];
var regSizes = sizes.splice(0, 3);
console.log(regSizes);

// ========= Task 6 ========= //

/* var pets = ["dog", "cat", "ox", "duck", "frog"]. Add 2 elements after "dog" and remove "cat", "ox", and "duck". */

var pets = ["dog", "cat", "ox", "duck", "frog"];
pets.splice(1, 5, "parrot", "dolphin");
console.log(pets);

// ========= Task 7 ========= //

/* var pets = ["dog", "cat", "ox", "duck", "frog"]; Remove "cat" and "ox". */

var pets = ["dog", "cat", "ox", "duck", "frog"];
pets.splice(1, 2);
console.log(pets);

// ========= Task 8 ========= //

/* var pets = ["dog", "cat", "ox", "duck", "frog", "flea"]; Reduce it to "duck" and "frog" using slice. */

var pets = ["dog", "cat", "ox", "duck", "frog", "flea"];
var myPets = pets.slice(3, 5);
console.log(myPets);

// <========= Chp 17-20 (for Loops) =========> //

// ========= Task 1 ========= //

/* Write a statement in which loop is to run 10 times. */

var a = "Hello World";
for (var i = 0; i < 10; i++) {
  console.log(a);
}

// ========= Task 2 ========= //

/* Code the first line of a for loop with the usual counter name, usual starting value, and usual increment. Run it 12 times using <= to specify how many loops. */

var b = "Count";
for (var i = 0; i <= 12; i++) {
  console.log(i + " <= " + b);
}

// ========= Task 3 ========= //

/* What are the 5 characters missing from this code, excluding any spaces that are missing? Type them in order, with no spaces or commas between them.for var i = 0 i <= 4 i++ Note: Complete this statement by yourself */

for (var i = 0; i <= 4; i++) {} // Round brackets, semi colons, curly bracket were missing.

// ========= Task 4 ========= //

/* Code the first line of a for loop with a counter name that's not i. Code the usual starting value and usual increment. Run it 100 times using < to specify how many loops. */

var a = "Count";
for (var x = 1; x <= 100; x++) {
  console.log(x + " < " + a);
}

// ========= Task 5 ========= //

/* Code the first line of a for loop with the usual counter and the usual starting value. Run it 3 times using > to specify how many loops. Decrement it with each iteration. */

var a = "Count";
for (var i = 3; i > 0; i--) {
  console.log(i + " > " + a);
}

// ========= Task 6 ========= //

/* The statement assigns the number of elements in the array to the variable. */

var arr = [1, 2, 3, 4, 5];
var arr1 = arr.length;
console.log(arr1);

// ========= Task 7 ========= //

/* Set a variable named “flag” with an initial Boolean value of your choice. */

var flag = false;

// ========= Task 8 ========= //

/* Code the first line of a for loop with the usual counter, the usual starting value, and the usual incrementing. Limit the number of loops by the number of elements in the array pets. */

var pets = ["dog", "cat", "ox", "duck", "frog", "flea"];
for (var i = 0; i < pets.length; i++) {}

// ========= Task 9 ========= //

/* Coding Exercise: 
Set a for loop to run 10 iterations. On the second iteration, display the counter in an alert. (It should be 1.) Break out of the loop. */

var a = 1;
for (var i = 0; i < 10; i++) {
  if (i == a) {
    alert("1 found");
    break;
  }
}

// ========= Task 10 ========= //

/* Create an array which contains user names Code a prompt with the message "Enter first name". The user's response is assigned to firstName. Code the first line of a for loop with the usual counter, the usual starting value, and the usual incrementing. Limit the number of loops by the number of elements in the array user names. Code an if statement that tests the presense of (user name) in an array.If user name match Alert that "Enter". if not then alert ("Please write correct user name"). */

var userNames = ["Shahzaib", "Ammad", "Arsalan", "Haris"];
var firstName = prompt("Enter first name");

for (var i = 0; i < userNames.length; i++) {
  if (firstName == userNames[i]) {
    alert("Enter");
    break;
  } else {
    alert("Please write correct user name");
  }
}

// ========= Task 11 ========= //

/* Complete this code to display an alert if a match isn't found.
var matchFound = false;
for (var i = 0; i < list.length; i++) {
if (userInput === list[i]) {
alert("Match found");
matchFound = true;
break;
}
}; */

var list = ["Apple", "Mango", "Banana"];
var userInput = prompt("Enter Any fruit name");
var matchFound = false;
for (var i = 0; i < list.length; i++) {
  if (userInput === list[i]) {
    alert("Match found");
    matchFound = true;
    break;
  } else {
    alert("Not Found");
  }
}

// ========= Task 12 ========= //

/* var firstArr = [“a”, “b”, “c”, “d”, “e”, “f”];
var secondArr = [1, 2, 3, 4, 5, 6];
Code the first line of a for loop with the usual counter, the usual
starting value, and the usual incrementing. Limit the number of
loops by the number of elements in the array firstArr.
In the scope of main loop Code the nested loop. Limit the number
of nested loops by the number of elements in the array
secondArr.
After that concatenate the both loops.
Expected Output:
a1
a2
a3
a4
...
f6 */

var firstArr = ["a", "b", "c", "d", "e", "f"];
var secondArr = [1, 2, 3, 4, 5, 6];
for(var i = 0; i <firstArr.length; i++){
   for(var j = i; j < secondArr.length; j++){
      console.log(firstArr[i], secondArr[j]);
   }
}

// <========= THE END =========> //